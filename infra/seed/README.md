# Seed Data

Sample data loaded on local startup:
- `products.json` — ≥ 20 sample eyewear products (with frame attributes).
- `face-shape-mapping.json` — face shape → recommended frame shapes mapping.
